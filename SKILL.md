---
name: bank-statement-pdf
description: 把"银行交易明细 CSV"按"原版对账单 PDF"的排版重建为内容与 CSV 完全一致的替换版 PDF（九江银行等模板）。触发场景：用户给出一份银行交易明细 CSV（如"2025年版_九江银行交易明细清单.csv"）和对应的原版 PDF（如"交易明细清单-2025.5.1-2025.12.31.pdf"），要求"按 CSV 重新生成 PDF / 明细跟 CSV 一样 / 保持原排版 / 更新替换版"。同时覆盖配套处理：Excel 另存导致的 CSV 损坏修复（日期丢秒/科学计数/账号丢位）、把交易时间秒数为 00 的行随机化为 01–59、以及生成后的像素级版式验证。
---

# 银行交易明细 CSV → 原排版 PDF 重建

把银行对账单"原版 PDF 的版式"与"CSV 的明细内容"合成一份新 PDF：布局/网格/字体/logo/二维码与原版一致，数据 100% 来自 CSV。

## 环境前提（Windows）

- Python 3.12+；依赖：`pymupdf`、`fonttools`、`Pillow`（`pip install pymupdf fonttools pillow`）
- 原版 PDF 内嵌字体为 STSong（TrueType 子集）。系统中 `C:\Windows\Fonts\STSONG.TTF`（华文宋体）与其**度量逐字符一致、字形像素一致**——用它做渲染并按需子集化嵌入。缺失时检查 simsun/其他宋体并告知用户字体近似度下降。
- 脚本目录：本 skill 的 `scripts/`（`make_statement.py`、`verify_statement.py`、`randomize_seconds.py`）。

## 标准流程

### 0. 输入定位

- 输入对：`<明细>.csv` + `<原版>.pdf`。常见摆放：与 CSV 同名/同目录、用户新文件在 `workshop/123/`、文件带 `(1)` 等下载后缀。用 `glob`/`ls` 找齐，别假设路径。
- 若存在新目录（如 `123/`）且与旧文件同名：以最新/用户指定者为准，可同步覆盖旧副本。

### 1. 数据预检（必做）

用 Python（`csv.reader`, `utf-8-sig`）解析：定位表头行（首列为"交易时间"），其上是键值头（查询账号/开户银行/账户名称/交易时间范围/借贷汇总）。逐行检查：

- 行数 & 每行字段数必须等于表头列数（10）。
- `交易时间` 必须匹配 `\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}`（不要 `2025/5/1 10:15` 这种）。
- 金额/余额/账号不得含科学计数（`[Ee][+-]?\d`）。
- 收入/支出互斥且有其一；币种=人民币。
- 逐行重算借贷笔数/总额，与键值头汇总核对（不一致则警告，以逐行为准）。

### 2. CSV 损坏修复（Excel 另存/手改的常见副作用）

用户常拿 Excel 编辑后另存，产生不可逆损坏。先与旧数据基线比对（基线来源：上一版替换版 PDF 文本逐行回读，或上一版完好 CSV），**只修复"损坏字段"**，其余以新 CSV 为准：

| 症状 | 修复 |
|---|---|
| 时间变 `2025/5/1 10:15`（斜杠/去零/丢秒） | 回基线补零与秒 |
| 对方账号/用途变 `6.217E+18`、`9.91521E+15` | 科学计数**丢位不可逆**，须按行从基线恢复原文 |
| 查询账号（键值头）变科学计数 | 从原版 PDF 首页文本恢复 |
| 金额丢 `+`/尾零 | 数值未损，格式化由生成器补回 |
| 时间秒为 `00` | 若用户要求随机：见步骤 3 |

规则：**只覆盖可疑单元格**，勿重写整行（曾因整行覆盖丢字段）；用户声明"只改了某列"时，其余列以基线为准并对齐校验（逐行打印差异）。

### 3. 随机秒（按需）

用户要求"交易时间秒数为 00 的都改为 01~59 随机"时：只替换该行时间字段的秒位子串，保留行尾其余内容。

```bash
python scripts/randomize_seconds.py "<明细>.csv"
```

### 4. 生成

```bash
python scripts/make_statement.py "<明细>.csv" "<原版.pdf>" "[输出.pdf]"
```

输出缺省名为输入 CSV 同目录 `<原版名>-明细按CSV替换版.pdf`。脚本行为：

- 版式常量来自模板实测（下表），并对每份原版 PDF 首页做**像素模板探测**（首条网格线 133.8±1、次条 146.8±1），不符即报错，防模板漂移。
- logo(118×30 左上)与防伪二维码(90×87.6 右上)从原版自动定位提取；二维码按原版 z 序画在行文字之上（仅模块像素覆盖，透明区不遮字）；logo 仅首页、二维码每页。
- 字体：`C:\Windows\Fonts\STSONG.TTF` → fontTools 子集化（按全部将写文本的字符集）→ 嵌入。CJK=9.0/字，ASCII=半宽(≈4.2–4.5)；数字右对齐右缘=列右界−2.0pt；其余列逐行居中；长文本按列可用宽 49.5pt 贪心折行，行内行距 9.0，首行基线=行顶+11.5。
- 数字列限宽 = 列宽−右留白；超宽才缩字号（≥7.0），STSONG 下 `±2,800,000.00`(≈50pt) 不必缩。
- 行高 = max(30, 9×行内文本行数+4)；首页数据区 146.8 起、续页 35.8 起；放不下自动翻页。

### 5. 验证（必做，全 PASS 才算完成）

```bash
python scripts/verify_statement.py "<明细>.csv" "<原版.pdf>" "<输出.pdf>"
```

- 内容：日期集合、时间数、全部金额/币种格式化值齐全。
- 几何：首页首行基线≈158.3；首行四列数字右缘=138.6/190.9/243.2/295.5（±0.3）。
- 网格：首页表头带 133.8/146.8、续页 35.8、列线 ≥11 条。
- 静态像素：与首页静态区(logo/标题/信息/表头)对比 logo=0.0、背景<0.005、顶部<0.12、表头<0.30（残余为表格线 0.05pt 级亚像素，允许）。

再加两条强校验（脚本不含、逐次执行）：

1. **逐行回读比对**：按列坐标把 PDF 文本行拼回 10 字段（时间/金额/收入/支出/余额/币种/账号/户名/摘要/用途），与 CSV 逐行比较——0 不一致。
2. 若做过随机秒：确认 CSV 与 PDF 中均无 `:00` 秒。

### 6. 交付

- 输出放原版 PDF 同目录，命名 `<原版名>-明细按CSV替换版.pdf`；把工作脚本留 `_work/` 或 skill `scripts/`。
- 汇报：页数/行数/借贷笔数与总额、自动修复清单（如有）、验证结果、与上版的差异（行数变化/用途改动）。

## 版式参数速查（模板实测值，供参考）

- 页 A4 595×842；10 等宽列，x = 36.0, 88.3, 140.6, 192.9, 245.2, 297.5, 349.8, 402.1, 454.4, 506.7, 559.0；列中心 = 相邻 x 均值。
- 首页：标题"交易明细清单"红 20pt 居中，基线 y=57；信息块 9pt 两列（x=52.5 / 312.5，基线 83/96/109/122）；表头带顶线 133.8、表头文字基线 145、首数据行顶 146.8，首页数据行容量 ≈21。
- 续页：表格顶线 35.8，无表头，容量 ≈25；列内容按行自动增高、边框随内容收底（末页边框在最后一行下沿）。
- 行距 30/40/49…=max(30, 9n+4)；行内文本行距 9.0；表头/信息/表格字号 9，标题 20。
- 网格：黑色 0.5pt 描边；二维码每页右上(450,44.4,540,132)；logo 首页左上(41,37,159,67)。

## 已知坑

- **覆盖被占用文件**：目标 PDF 正被查看器打开 → `PermissionError`；改存临时文件后 `os.replace`，失败则重试并提示用户关闭。
- **编辑 CSV 勿整行替换**：只改目标子串，行尾字段保留（本流程曾因此丢 8 行数据）。
- **千分位**：合并/修复出的金额可能含 `,`（如 `50,000.00`），脚本内 float 解析一律先 `replace(',','')`。
- **金额符号**：CSV 交易金额可能无 `+`；`fmt_amount` 按负号判断，正数自动加 `+`。
- **字符集**：子集字体务必覆盖全部写出的字符（标题/信息/表头/每行每列拼好的完整字符串+ASCII 标点），漏字符会出豆腐块。
- **行锚**：PDF 回读以日期 span 为行锚，账号/摘要/用途多行折行需按列(x 区间)拼接，勿按页面顺序直拼。
